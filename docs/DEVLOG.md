# Devlog

## 2026-03-09

### 네이티브 전환

- 웹 중심 MVP에서 `Pygame` 네이티브 전투 클라이언트를 기본 플레이 경로로 승격
- `.venv` 기반 실행 흐름과 `requirements-native.txt` 추가

### 비주얼 개편 1차

- 한국어 폰트 번들 추가
- 전장 배경, 패널 스타일, 캐릭터 실루엣, 스킬 연출 재구성
- 헤드리스 스크린샷 출력 경로 추가

### 전투 UX 보정

- 적 대상 스킬의 유효 타겟 계산 버그 수정
- 아리와 징크스 같은 단일 대상 스킬이 정상적으로 적 클릭을 받도록 수정
- 챔피언 아이콘 에셋을 받아 전장 유닛과 사이드 패널 비주얼을 개선

### 오디오 추가

- `native_game/audio.py`로 절차형 효과음 생성기 추가
- 버튼 선택, 시전, 피격, 보호막, 기절, 승패 사운드 연결
- 낮은 볼륨의 앰비언트 루프 추가

### WSL 오디오 런타임 보정

- 시스템에 `libpulse0`가 없는 환경을 확인
- `.vendor/pulse` 아래에 필요한 PulseAudio 관련 라이브러리를 번들
- `native_game/__main__.py`에서 실행 전 `LD_LIBRARY_PATH`와 `SDL_AUDIODRIVER`를 보정하도록 추가
- `npm run native:play`도 같은 경로를 사용하도록 보강

### 챔피언 선택 화면 추가

- 게임 시작 시 바로 전투가 아니라 챔피언 선택 화면으로 진입하도록 변경
- 블루 팀 풀에 럭스, 바이 추가
- 레드 팀 풀에 모르가나, 야스오 추가
- 플레이어는 5명 중 3명을 선택하고, 적은 5명 중 3명이 재추첨 가능하게 구성
- 선택된 라인업이 실제 전투 컨트롤러에 반영되도록 `BattleController`를 확장

### 로스터 2차 확장

- 블루 팀 풀에 이즈리얼, 레오나 추가
- 레드 팀 풀에 제드, 리산드라 추가
- 선택 화면 카드 그리드를 3열 대응으로 바꿔 7인 풀에서도 화면이 무너지지 않도록 조정
- 새 로스터를 검증하는 테스트 케이스 추가

### 실행 파일 패키징

- `PyInstaller` 기반 패키징 경로 추가
- `native_game/runtime.py`로 일반 실행과 번들 실행의 리소스 경로를 통합
- `release/rift-tactics` 단일 실행 파일 생성 및 헤드리스 스크린샷 검증 완료
- Windows 러너용 `.exe` 빌드 스크립트와 GitHub Actions 워크플로 추가

### 챔피언 테마 사운드

- 공식 게임 음원을 쓰지 않고 챔피언 성격별 오리지널 신스 모티프를 구성
- 챔피언 선택 카드 클릭과 전투 시전에 개별 테마 사운드 연결
- 일반 타격, 보호막, 기절, 승패 사운드는 기존 전투 피드백 레이어로 유지

### 로스터 3차 확장

- 블루 팀 풀에 애쉬, 브라움 추가
- 레드 팀 풀에 카타리나, 브랜드 추가
- 선택 가능 로스터를 9대9로 확장
- 새 챔피언 아이콘과 테마 사운드를 함께 추가

### 전술 맵 실험 분기 시작

- 기존 `native_game` 아레나 전투는 유지
- 새 `native_tactics` 패키지를 추가해 8x6 그리드 전술 슬라이스 구현
- 이동 1회 + 행동 1회 구조, 장애물 타일, 사거리 하이라이트, 간단한 적 AI 추가
- `tactics:play`, `tactics:test`, `tactics:smoke`, `tactics:capture` 스크립트 추가
- 헤드리스 스크린샷으로 첫 전술 맵 UI 결과 검증

### 전술 재미 로드맵 정리

- 전투 설계, 시스템, UX, 콘텐츠, 리텐션 관점으로 `native_tactics` 재미 요소를 재정리
- 다음 핵심 우선순위를 `챔피언 선택 -> 시작 배치 -> 고유 패시브 -> 지형/의도 표시 -> 전투 후 보상`으로 고정
- 로드맵 문서를 `docs/TACTICS_FUN_ROADMAP.md`로 추가

### 전술 선택/배치 흐름 추가

- `native_tactics`를 전투 직행형에서 `챔피언 선택 -> 시작 배치 -> 전투` 흐름으로 확장
- 블루 팀은 9인 풀에서 3명을 선택하고, 적 팀은 9인 풀에서 3명이 재추첨되도록 구성
- 전투 전 파란 시작 칸 위에서 플레이어가 직접 시작 위치를 재배치할 수 있도록 추가
- 전술 컨트롤러가 커스텀 시작 좌표를 받아 리셋 시에도 같은 배치 기준을 유지하도록 보강
- 헤드리스 환경에서 `선택 -> 배치 -> 전투` 상태 전환을 직접 검증

### 작업 운영 문서화

- `CLAUDE.md` 추가
- `tasks/todo.md`, `tasks/lessons.md` 추가
- `PROJECT_CONTEXT.md`를 인계 기준점으로 유지
